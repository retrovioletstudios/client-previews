<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8"/>
<meta name="viewport" content="width=device-width, initial-scale=1.0"/>
<title>The Atypical Mechanic | Mobile Auto Repair</title>
<meta name="description" content="Mobile Mechanics You Can Trust. ASE Certified repairs delivered to Austin, Round Rock, Pflugerville, and beyond."/>
<link href="https://fonts.googleapis.com/css2?family=Bebas+Neue&family=Barlow:wght@400;500;600;700&family=Barlow+Condensed:wght@500;700&display=swap" rel="stylesheet"/>
<style>
  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
  :root {
    /* Slate Grey & Barely Green Palette */
    --accent: #ACB7AE; 
    --accent-dark: #8A968C; 
    --highlight: #FFFFFF;
    --dark: #1E2321; 
    --dark2: #28302D; 
    --dark3: #5C766D; /* Slate Grey */
    --light: #E8E8E8; --white: #FFFFFF;
    --font-head: 'Bebas Neue', sans-serif;
    --font-body: 'Barlow', sans-serif;
    --font-sub: 'Barlow Condensed', sans-serif;
  }
  body { font-family: var(--font-body); background: var(--dark); color: var(--white); overflow-x: hidden; }

  nav {
    background: rgba(30,35,33,0.97); border-bottom: 1px solid var(--dark3);
    display: flex; align-items: center; justify-content: space-between;
    padding: 0 2rem; height: 60px; position: sticky; top: 0; z-index: 100;
  }
  .nav-logo { font-family: var(--font-head); font-size: 24px; letter-spacing: 2px; color: var(--white); display: flex; align-items: center; gap: 8px; }
  .nav-logo span { color: var(--accent); }
  .nav-loc { font-family: var(--font-sub); font-size: 12px; color: var(--accent); letter-spacing: 1px; text-transform: uppercase; padding-left: 10px; border-left: 1px solid var(--dark3); }
  .nav-links { display: flex; gap: 1.75rem; }
  .nav-links a { font-family: var(--font-sub); font-size: 14px; font-weight: 700; letter-spacing: 1.5px; text-transform: uppercase; color: var(--light); text-decoration: none; cursor: pointer; border-bottom: 2px solid transparent; padding-bottom: 2px; transition: color 0.2s; }
  .nav-links a:hover, .nav-links a.active { color: var(--accent); border-color: var(--accent); }
  .nav-cta { background: var(--dark3); color: var(--white); font-family: var(--font-sub); font-weight: 700; font-size: 13px; letter-spacing: 2px; text-transform: uppercase; border: none; padding: 9px 20px; cursor: pointer; transition: background 0.2s; }
  .nav-cta:hover { background: var(--accent); color: var(--dark); }

  .page { display: none; }
  .page.active { display: block; }

  /* Minimalist Hero */
  .hero { position: relative; background: var(--dark); padding: 5rem 2rem 4rem; overflow: hidden; display: flex; justify-content: center; }
  .hero-content { position: relative; z-index: 2; max-width: 700px; text-align: center; }
  .hero-tag { display: inline-block; background: transparent; color: var(--accent); border: 1px solid var(--accent); font-family: var(--font-sub); font-size: 12px; font-weight: 700; letter-spacing: 3px; text-transform: uppercase; padding: 4px 12px; margin-bottom: 1.5rem; }
  .hero h1 { font-family: var(--font-head); font-size: clamp(48px, 8vw, 84px); line-height: 0.92; letter-spacing: 2px; text-transform: uppercase; margin-bottom: 1.5rem; }
  .hero h1 em { color: var(--accent); font-style: normal; }
  .hero-sub { font-size: 17px; color: #CCC; max-width: 540px; line-height: 1.6; margin-bottom: 2rem; margin-left: auto; margin-right: auto; }
  .hero-sub strong { color: var(--white); }
  
  .hero-btns { display: flex; gap: 1rem; flex-wrap: wrap; justify-content: center; margin-bottom: 3rem; }
  .btn-primary { background: var(--white); color: var(--dark); font-family: var(--font-sub); font-weight: 700; font-size: 15px; letter-spacing: 2px; text-transform: uppercase; border: none; padding: 15px 32px; cursor: pointer; transition: all 0.2s; }
  .btn-primary:hover { background: var(--accent); }
  
  .btn-outline { background: transparent; color: var(--white); font-family: var(--font-sub); font-weight: 700; font-size: 14px; letter-spacing: 2px; text-transform: uppercase; border: 1px solid var(--dark3); padding: 14px 28px; cursor: pointer; transition: all 0.2s; }
  .btn-outline:hover { border-color: var(--accent); color: var(--accent); }
  
  .hero-stats { display: flex; gap: 3rem; justify-content: center; padding-top: 2rem; border-top: 1px solid var(--dark2); }
  .hero-stat-num { font-family: var(--font-head); font-size: 36px; color: var(--accent); line-height: 1; }
  .hero-stat-label { font-family: var(--font-sub); font-size: 11px; letter-spacing: 1px; text-transform: uppercase; color: #999; margin-top: 5px; }

  .section { padding: 4rem 2rem; }
  .section-inner { max-width: 1000px; margin: 0 auto; }
  .section-label { font-family: var(--font-sub); font-size: 12px; font-weight: 700; letter-spacing: 3px; text-transform: uppercase; color: var(--accent); margin-bottom: 0.5rem; }
  .section-title { font-family: var(--font-head); font-size: clamp(32px, 5vw, 48px); letter-spacing: 1px; text-transform: uppercase; line-height: 1; margin-bottom: 1.5rem; }

  /* 4 Pillars Grid */
  .services-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 2px; background: var(--dark3); border: 2px solid var(--dark3); margin-top: 2.5rem; }
  @media (max-width: 900px) { .services-grid { grid-template-columns: repeat(2, 1fr); } }
  @media (max-width: 500px) { .services-grid { grid-template-columns: 1fr; } }
  .service-card { background: var(--dark2); padding: 2.5rem 1.5rem; transition: background 0.2s; text-align: center; }
  .service-card:hover { background: var(--dark); }
  .service-icon { font-size: 32px; margin-bottom: 1rem; opacity: 0.9; }
  .service-name { font-family: var(--font-sub); font-size: 18px; font-weight: 700; letter-spacing: 1.5px; text-transform: uppercase; margin-bottom: 0.5rem; color: var(--accent); }
  .service-desc { font-size: 14px; color: #AAA; line-height: 1.6; }

  .services-page-hero { background: var(--dark2); border-bottom: 1px solid var(--dark3); padding: 4rem 2rem; text-align: center; }
  .services-big-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 2rem; margin-top: 3rem; text-align: left;}
  .service-big-card { background: var(--dark2); border: 1px solid var(--dark3); padding: 2rem; transition: transform 0.2s; position: relative; }
  .service-big-card:hover { border-color: var(--accent); transform: translateY(-3px); }
  .service-price { font-family: var(--font-head); font-size: 28px; color: var(--white); margin-bottom: 0.5rem; letter-spacing: 1px;}
  .service-features { list-style: none; margin: 1.5rem 0; }
  .service-features li { font-size: 14px; color: #CCC; padding: 8px 0; border-bottom: 1px solid rgba(255,255,255,0.05); display: flex; align-items: center; gap: 10px; }
  .service-features li::before { content: '■'; color: var(--accent); font-size: 8px; }

  .areas-row { display: flex; flex-wrap: wrap; gap: 0.75rem; margin-top: 2rem; justify-content: center;}
  .area-pill { font-family: var(--font-sub); font-size: 14px; font-weight: 700; letter-spacing: 1px; text-transform: uppercase; color: var(--white); background: var(--dark3); padding: 8px 18px; border-radius: 20px;}

  .about-split { display: grid; grid-template-columns: 1fr 1fr; gap: 4rem; align-items: center; margin-top: 3rem; text-align: left; }
  @media (max-width: 768px) { .about-split, .contact-grid { grid-template-columns: 1fr; } }
  .about-img-mock { background: var(--dark2); border: 1px solid var(--dark3); aspect-ratio: 1/1; display: flex; flex-direction: column; align-items: center; justify-content: center; position: relative; }
  .about-img-mock div { font-family: var(--font-head); font-size: 80px; color: var(--accent); line-height: 1; }
  .about-img-mock span { font-family: var(--font-sub); letter-spacing: 3px; font-size: 14px; color: #888; margin-top: 10px; }
  .about-text p { font-size: 16px; color: #BBB; line-height: 1.8; margin-bottom: 1.2rem; }
  .about-text strong { color: var(--white); font-weight: 600; }

  .contact-grid { display: grid; grid-template-columns: 1fr 1.2fr; gap: 4rem; margin-top: 3rem; align-items: start; text-align: left;}
  .contact-info-block { margin-bottom: 2.5rem; }
  .contact-info-label { font-family: var(--font-sub); font-size: 12px; font-weight: 700; letter-spacing: 3px; text-transform: uppercase; color: var(--accent); margin-bottom: 0.5rem; }
  .contact-info-val { font-size: 16px; color: #E8E8E8; line-height: 1.6; }
  
  .form-group { margin-bottom: 1.25rem; }
  .form-label { display: block; font-family: var(--font-sub); font-size: 12px; font-weight: 700; letter-spacing: 2px; text-transform: uppercase; color: #999; margin-bottom: 8px; }
  .form-input, .form-select, .form-textarea { width: 100%; background: var(--dark); border: 1px solid var(--dark3); color: var(--white); font-family: var(--font-body); font-size: 15px; padding: 14px 16px; outline: none; transition: border-color 0.2s; appearance: none; }
  .form-input:focus, .form-select:focus, .form-textarea:focus { border-color: var(--accent); }
  .form-textarea { resize: vertical; min-height: 120px; }
  .form-select option { background: var(--dark); }
  .form-row { display: grid; grid-template-columns: 1fr 1fr; gap: 1.25rem; }
  .submit-confirm { background: var(--dark3); color: var(--white); padding: 1rem 1.5rem; font-family: var(--font-sub); font-size: 15px; letter-spacing: 1px; margin-top: 1rem; display: none; text-align: center; }

  footer { background: #181C1A; border-top: 1px solid var(--dark3); padding: 3rem 2rem; text-align: center; }
  .footer-logo { font-family: var(--font-head); font-size: 24px; letter-spacing: 3px; margin-bottom: 0.5rem; color: var(--white); }
  .footer-address { font-size: 14px; color: #888; margin-bottom: 0.5rem; }
  .sr-only { position: absolute; width: 1px; height: 1px; overflow: hidden; clip: rect(0,0,0,0); }
</style>
</head>
<body>

<h2 class="sr-only">The Atypical Mechanic — Mobile Mechanics You Can Trust. ASE Certified repairs.</h2>

<nav>
  <div class="nav-logo">ATYPICAL<span>MECHANIC</span></div>
  <div class="nav-links">
    <a onclick="showPage('home')" class="active" id="nav-home">Home</a>
    <a onclick="showPage('services')" id="nav-services">Our Pillars</a>
    <a onclick="showPage('about')" id="nav-about">About Us</a>
    <a onclick="showPage('contact')" id="nav-contact">Free Estimate</a>
  </div>
  <a href="tel:8009186160" class="nav-cta" style="text-decoration: none;">(800) 918-6160</a>
</nav>

<div class="page active" id="page-home">

  <section class="hero">
    <div class="hero-content">
      <div class="hero-tag">Clear. Honest. Mobile.</div>
      <h1>Mobile Mechanics<br>You Can <em>Trust</em>.</h1>
      <p class="hero-sub"><strong>Unlock the secret to your vehicle's longevity.</strong><br><br>We are fully equipped mobile vans operated by ASE Certified mechanics. Get dealership-quality repairs at your home or office—saving an average of 30% over physical shops.</p>
      <div class="hero-btns">
        <button class="
